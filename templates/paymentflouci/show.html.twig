{% extends 'base.html.twig' %}

{% block title %}Paymentflouci{% endblock %}

{% block body %}
    <h1>Paymentflouci</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paymentflouci.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paymentflouci.montant }}</td>
            </tr>
            <tr>
                <th>Transaction_id</th>
                <td>{{ paymentflouci.transactionId }}</td>
            </tr>
            <tr>
                <th>Payment_status</th>
                <td>{{ paymentflouci.PaymentStatus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paymentflouci_index') }}">back to list</a>

    <a href="{{ path('app_paymentflouci_edit', {'id': paymentflouci.id}) }}">edit</a>

    {{ include('paymentflouci/_delete_form.html.twig') }}
{% endblock %}
