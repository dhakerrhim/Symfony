{% extends 'base.html.twig' %}

{% block title %}Paymentflouci index{% endblock %}

{% block body %}
    <h1>Paymentflouci index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Montant</th>
                <th>Transaction_id</th>
                <th>Payment_status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for paymentflouci in paymentfloucis %}
            <tr>
                <td>{{ paymentflouci.id }}</td>
                <td>{{ paymentflouci.montant }}</td>
                <td>{{ paymentflouci.transactionId }}</td>
                <td>{{ paymentflouci.PaymentStatus }}</td>
                <td>
                    <a href="{{ path('app_paymentflouci_show', {'id': paymentflouci.id}) }}">show</a>
                    <a href="{{ path('app_paymentflouci_edit', {'id': paymentflouci.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_paymentflouci_new') }}">Create new</a>
{% endblock %}
